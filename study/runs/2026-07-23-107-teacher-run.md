# 107 教师先行运行记录（2026-07-23）

这不是学生应照抄的实验结论，而是教程作者先走完整条环境、正确性、benchmark 与 profiling
路径的证据。原始大日志留在远端 `artifacts/`，本文件只保留能复核的关键信息。

## 固定对象

```text
tutorial branch: codex/tutorial-book
tutorial commits: 68bbf35 -> 3f7c540 -> dacec5a
FLA repo: ~/sparse_linear/flash-linear-attention
FLA commit: d1ce07369d581813553f30a750af3b6b5f9af6a9
environment: ~/sparse_linear/.envs/sla-tutorial-py312
node observed: anode05
GPU: NVIDIA GeForce RTX 5090, 32607 MiB, compute capability 12.0
driver: 580.159.03
Python: 3.12.13 (conda-forge prefix)
PyTorch: 2.11.0+cu128
PyTorch CUDA runtime: 12.8
Triton: 3.6.0
```

## 环境路径及失败证据

| job | 状态 | 观察 | 教程因此怎样修改 |
| --- | --- | --- | --- |
| `26146` | completed | 探测到 RTX 5090/driver 580.159.03 | 选官方 cu128 wheel，不猜节点 GPU |
| `26147` | failed | 系统 Python 没有 `ensurepip`/venv 支持 | 不依赖系统 venv |
| `26148` | failed | Miniconda 默认 channels 要求交互接受 ToS | 不代用户接受；改用 `--override-channels -c conda-forge` |
| `26150` | failed | 数 GB wheel 写 NFS prefix 时出现 `OSError: [Errno 2]` | cache/tmp 转到节点本地目录 |
| `26159` | failed | torch/Triton 已装，但下一次 pip 启动时 NFS cwd 消失，`os.getcwd()` 失败 | pip 的 cwd 也转到节点本地；只用绝对路径访问源码/回写记录 |
| `26170` | completed | 环境、FLA editable install、CUDA import 全部通过；`pip check` 无 broken requirements | setup gate 通过 |

结论：日志出现 `Successfully installed` 不能证明作业成功；必须同时看 `sacct State/ExitCode`
和最后的 import/environment report。遇到 NFS 可见性问题先 `cd .`，长安装/分析从本地 tmp
运行。

## 课程 smoke 与基准

job `26181`，tutorial commit `68bbf35`，状态 `COMPLETED (0:0)`：

- GPU 上 `13 passed`，包含本地 CPU 环境会 skip 的 Triton vector add；
- 教学版 GDN 与广播 scalar gate 的 KDA：output/state 最大差均为 0；
- per-channel gate 与 GDN output 最大差约 `0.6637`；
- `B=1,H=4,T=512,D=64,BF16` dense prefill：reference p50 约 `0.2055 ms`，
  torch SDPA p50 约 `0.03757 ms`；
- 同 context 的单 query decode：reference p50 约 `0.2072 ms`，torch SDPA p50 约
  `0.03808 ms`。

这些数字只是 smoke shape，不是最终性能结论。JSON 保存了每个样本、GPU、版本和 commit。

## FLA GDN/KDA 探针怎样被修正

| job | 状态 | 观察 |
| --- | --- | --- |
| `26182` | completed，但证据无效 | 未归一化随机 q/k 让递推放大；相对容差通过，最大绝对误差却达 `3--18` |
| `26185` | failed | `naive_recurrent_kda` 文件存在，但没有从 `fla.ops.kda` 公共层导出 |
| `26190` | completed | 从 `fla.ops.kda.naive` 导入，按官方测试 L2-normalize q/k，完成四方比较 |

job `26190` 的最大绝对误差：

```text
naive KDA vs naive GDN: output 1.49e-08, state 5.96e-08
fused GDN vs naive GDN: output 2.98e-08, state 1.19e-07
fused KDA vs naive KDA: output 2.24e-08, state 5.96e-08
fused GDN vs scalarized fused KDA: output 2.98e-08, state 5.96e-08
```

这支持的命题仅是：在此算子契约和 shape 下，将 KDA 的 key-channel log decay 广播为同一
head scalar 后，递推退化为 GDN。它不证明完整 Kimi Linear 等于 GDN，也不是性能/质量结论。

## Profiling 工具链

job `26199`，tutorial commit `dacec5a`，状态 `COMPLETED (0:0)`：

```text
nsys: NOT FOUND
ncu: /usr/bin/ncu, version 2022.4.1
compute-sanitizer: version 2022.4.1
nvprof: 12.0.146 (legacy; not used as the course path)
PyTorch Chrome trace: 88 KiB
```

PyTorch profiler 在固定 SDPA prefill 中观察到每步两个 GPU kernel：flash split-KV kernel 与
combine kernel；5 步对应 10 次 `cudaLaunchKernel`。Profiler 还显示 activity-buffer 等
插桩成本，因此其时间不替代 `26181` 的 CUDA Event benchmark。

job `26205` 验证了移除无 schedule 的显式 `prof.step()` 后 trace 内容仍正确；PyTorch 2.11
仍在结束默认 cycle 时提示事件清理，因此最终脚本显式使用 `acc_events=True`，再由后续 job
做无警告验收。

job `26200` 首次运行 `ncu --set basic` 失败：旧版没有该 set，并出现旧注入库与当前进程的
兼容错误。进一步运行 `ncu --list-chips` 只得到 `ad10x/ga10x/gh100/...`，没有 RTX 5090
的 `gb202`。所以当前 5090 节点不能用这套 2022.4.1 工具采硬件 counter；修订脚本会在尝试
前明确跳过。以后分到 A100 时重新检查（该版本列出了 `ga100`），再区分架构支持与
performance-counter 权限。

job `26207` 验证最终预检：作业以 `COMPLETED (0:0)` 结束，完整打印 ncu 版本和 chip list，
随后输出 `ncu_status=SKIPPED`，没有再把不兼容工具注入 PyTorch 进程。

job `26210` 在 commit `0e9ac6f` 上完成最终 PyTorch trace 验收：`COMPLETED (0:0)`、stderr
为空、5 个 SDPA step 对应 10 次 CUDA launch，trace 为 89 KiB；`acc_events=True` 后不再
出现 cycle 事件清理警告。

## 学生从哪里接手

1. 从教程关卡 A 开始，不重跑全部失败 job；失败表用于理解环境决策。
2. 完成 TODO/grader 和自己的 run note，不修改本文件来假装完成。
3. benchmark 扩大 shape 前先写问题、预测和停止条件。
4. profiler 先用 PyTorch trace；只有目标节点实际提供兼容的 `nsys/ncu` 才进入下一层。
